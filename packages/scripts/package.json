{
  "name": "@rasika/scripts",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "//": "type: module matches core, trpc and web. Without it tsx loaded these scripts as CommonJS and resolved dependencies under the `require` condition — and @openauthjs/openauth exports only `import`, so every script importing @rasika/core died on ERR_PACKAGE_PATH_NOT_EXPORTED before running a line. That took out the whole CLI, backfills included.",
  "type": "module",
  "scripts": {
    "cli": "sst shell -- tsx src/cli.ts",
    "prod-cli": "sst shell --stage prod -- tsx src/cli.ts",
    "shell": "sst shell tsx",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1000.0",
    "@aws-sdk/client-s3": "^3.1000.0",
    "@aws-sdk/lib-dynamodb": "^3.1000.0",
    "@rasika/core": "workspace:*",
    "commander": "^14.0.3",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
